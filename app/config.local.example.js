// Copy this file to app/config.local.js and fill in real values.
// app/config.local.js is gitignored — never commit real API keys.
window.KEYSTONE_CONFIG = {
  apiKey: 'YOUR_GOOGLE_API_KEY',
  sheetId: 'YOUR_SHEET_ID',
  // Reuses the existing OAuth client from the nyra-bhajans Cloud project.
  oauthClientId: 'TODO_REPLACE_WITH_OAUTH_CLIENT_ID',
};
