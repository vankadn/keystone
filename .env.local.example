# Copy this file to .env.local and fill in real values. .env.local is
# gitignored (matches the *.local pattern) — never commit real keys.
VITE_API_KEY=YOUR_GOOGLE_API_KEY
VITE_SHEET_ID=YOUR_SHEET_ID
# Reuses the existing OAuth client from the nyra-bhajans Cloud project.
VITE_OAUTH_CLIENT_ID=TODO_REPLACE_WITH_OAUTH_CLIENT_ID
